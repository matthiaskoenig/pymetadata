"""Work in progress, not part of the public API.

Modules in here are drafts which are still baking: they are not wired into the
package, not covered by tests and not documented, and they can change or
disappear in any release. Do not import them from `pymetadata` code.

The package is excluded from the wheel and the sdist, it only exists in the
repository.
"""
