# Release notes for pymetadata 0.6.6
![pymetadata](./docs/images/favicon/pymetadata-100x100-300dpi.png)

## Fixes

- Write web service caches atomically so concurrent Python test environments and application processes do not read partial JSON. Failed writes preserve the previous cache (#91).

Your pymetadata team
